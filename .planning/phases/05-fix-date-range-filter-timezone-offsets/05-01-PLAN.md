---
phase: 05-fix-date-range-filter-timezone-offsets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app.js
  - tests/fixtures/sample-results-tz-offsets.json
autonomous: true
requirements:
  - UI-08
gap_closure: true

must_haves:
  truths:
    - "Date-from filter correctly excludes events whose UTC time is before the selected date, even when the raw timestamp string contains a negative offset making it look like it falls on that date"
    - "Date-to filter correctly includes events whose UTC time is within the selected end-of-day, even when the raw timestamp string contains a positive offset making it look like it falls on the next day"
    - "Events with Z-suffixed timestamps continue to filter correctly (no regression)"
  artifacts:
    - path: "web/app.js"
      provides: "Date filter logic using Date object comparison instead of string comparison"
      contains: "new Date"
    - path: "tests/fixtures/sample-results-tz-offsets.json"
      provides: "Test fixture with offset timestamps for manual verification"
  key_links:
    - from: "web/app.js:getFilteredSortedEvents"
      to: "filterState.dateFrom / filterState.dateTo"
      via: "Date object epoch comparison"
      pattern: "new Date\\(.*\\)\\.getTime\\(\\)"
---

<objective>
Fix the all-events date-range filter so it correctly handles ISO 8601 timestamps with timezone offsets (e.g. `2026-03-19T23:45:00-04:00`). Currently the filter uses string comparison (`>=` / `<=`) of raw timestamp strings against date boundary sentinels. This fails near day boundaries when offsets shift the actual UTC time into a different calendar day than the string prefix suggests.

Purpose: Close the UI-08 edge case gap -- date filtering must work regardless of timestamp offset format.
Output: Corrected `getFilteredSortedEvents()` in app.js, test fixture for verification.
</objective>

<execution_context>
@F:/ai/TheSmith/.planning/phases/05-fix-date-range-filter-timezone-offsets/05-01-PLAN.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current date filter logic in web/app.js (lines 624-636) that must be replaced -->

```javascript
// Apply date-from filter
if (filterState.dateFrom) {
    results = results.filter(function(evt) {
        return evt.timestamp >= filterState.dateFrom;
    });
}

// Apply date-to filter (include the full end day)
if (filterState.dateTo) {
    var endOfDay = filterState.dateTo + 'T23:59:59Z';
    results = results.filter(function(evt) {
        return evt.timestamp <= endOfDay;
    });
}
```

<!-- filterState shape (line 54) -->
```javascript
var filterState = { search: '', country: '', status: '', dateFrom: '', dateTo: '' };
```

<!-- dateFrom/dateTo values come from HTML date inputs, producing strings like "2026-03-19" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix date-range filter to use Date object comparison</name>
  <files>web/app.js</files>
  <read_first>web/app.js (full file -- focus on getFilteredSortedEvents function around lines 596-643)</read_first>
  <behavior>
    - Test 1: Timestamp "2026-03-19T23:45:00-04:00" (UTC: 2026-03-20T03:45:00Z) with dateFrom="2026-03-20" INCLUDED (it is on March 20 UTC)
    - Test 2: Timestamp "2026-03-19T23:45:00-04:00" (UTC: 2026-03-20T03:45:00Z) with dateTo="2026-03-19" EXCLUDED (it is past March 19 UTC)
    - Test 3: Timestamp "2026-03-20T01:30:00+05:00" (UTC: 2026-03-19T20:30:00Z) with dateTo="2026-03-19" INCLUDED (it is on March 19 UTC)
    - Test 4: Timestamp "2026-03-18T08:15:00Z" with dateFrom="2026-03-18" INCLUDED (Z timestamps still work)
    - Test 5: Timestamp "2026-03-18T08:15:00Z" with dateTo="2026-03-18" INCLUDED (Z timestamps still work)
  </behavior>
  <action>
Replace the two date-filter blocks inside `getFilteredSortedEvents()` in web/app.js.

CURRENT CODE (lines ~624-636):
```javascript
if (filterState.dateFrom) {
    results = results.filter(function(evt) {
        return evt.timestamp >= filterState.dateFrom;
    });
}
if (filterState.dateTo) {
    var endOfDay = filterState.dateTo + 'T23:59:59Z';
    results = results.filter(function(evt) {
        return evt.timestamp <= endOfDay;
    });
}
```

REPLACEMENT CODE:
```javascript
// Apply date-from filter (parse to UTC epoch for offset-safe comparison)
if (filterState.dateFrom) {
    var fromEpoch = new Date(filterState.dateFrom + 'T00:00:00Z').getTime();
    results = results.filter(function(evt) {
        return new Date(evt.timestamp).getTime() >= fromEpoch;
    });
}

// Apply date-to filter (include the full end day in UTC)
if (filterState.dateTo) {
    var toEpoch = new Date(filterState.dateTo + 'T23:59:59.999Z').getTime();
    results = results.filter(function(evt) {
        return new Date(evt.timestamp).getTime() <= toEpoch;
    });
}
```

Key points:
- `new Date("2026-03-19T23:45:00-04:00")` correctly parses to UTC epoch 2026-03-20T03:45:00Z
- `new Date("2026-03-19T00:00:00Z")` creates the UTC start-of-day boundary
- `new Date("2026-03-19T23:59:59.999Z")` creates the UTC end-of-day boundary
- Comparing `.getTime()` (epoch ms) is offset-safe -- no string comparison ambiguity
- Use `.999Z` milliseconds for end-of-day to capture the full final second

Do NOT change any other filter logic (search, country, status). Do NOT change the sort logic. Do NOT add new functions -- keep the fix inline within `getFilteredSortedEvents()`.
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'F:/ai/TheSmith/web/app.js' -Pattern 'new Date\(' | Measure-Object | ForEach-Object { if ($_.Count -ge 4) { 'PASS: Date objects used in filter logic' } else { 'FAIL: Expected at least 4 new Date() calls' } }"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `evt.timestamp >= filterState.dateFrom` returns zero matches (old string comparison removed)
    - grep for `evt.timestamp <= endOfDay` returns zero matches (old string comparison removed)
    - grep for `new Date(evt.timestamp).getTime()` returns exactly 2 matches (one for dateFrom, one for dateTo)
    - grep for `new Date(filterState.dateFrom` returns exactly 1 match
    - grep for `new Date(filterState.dateTo` returns exactly 1 match
    - grep for `T23:59:59.999Z` returns exactly 1 match
    - grep for `T00:00:00Z` returns exactly 1 match (in dateFrom boundary)
    - All other filter logic (search, country, status) is unchanged
    - The formatTimestamp function (line 25-27) is unchanged
  </acceptance_criteria>
  <done>The date-from and date-to filters in getFilteredSortedEvents() use new Date().getTime() epoch comparison instead of string comparison. Timestamps with timezone offsets are correctly normalized to UTC before boundary checking.</done>
</task>

<task type="auto">
  <name>Task 2: Create timezone-offset test fixture for manual verification</name>
  <files>tests/fixtures/sample-results-tz-offsets.json</files>
  <read_first>tests/fixtures/sample-results.json (first 50 lines -- understand the result object shape)</read_first>
  <action>
Create a small test fixture file at `tests/fixtures/sample-results-tz-offsets.json` containing 5 result objects that exercise the day-boundary edge cases. This fixture documents the expected behavior and can be loaded manually into the UI for verification.

Use this exact structure (matching the shape from sample-results.json):

```json
{
  "metadata": {
    "foreignEvents": 5,
    "totalEvents": 5,
    "truncationWarning": false
  },
  "results": [
    {
      "userPrincipalName": "tz.test@contoso.com",
      "userDisplayName": "TZ Test User",
      "ipAddress": "203.0.113.1",
      "country": "CN",
      "city": "Beijing",
      "timestamp": "2026-03-19T23:45:00-04:00",
      "appDisplayName": "Microsoft Teams",
      "clientAppUsed": "Browser",
      "isLegacyAuth": false,
      "signInStatus": "Success",
      "errorCode": "0",
      "riskLevel": "none",
      "deviceName": "",
      "deviceOS": "",
      "complianceState": ""
    },
    {
      "userPrincipalName": "tz.test@contoso.com",
      "userDisplayName": "TZ Test User",
      "ipAddress": "203.0.113.2",
      "country": "RU",
      "city": "Moscow",
      "timestamp": "2026-03-20T01:30:00+05:00",
      "appDisplayName": "Outlook",
      "clientAppUsed": "Browser",
      "isLegacyAuth": false,
      "signInStatus": "Success",
      "errorCode": "0",
      "riskLevel": "none",
      "deviceName": "",
      "deviceOS": "",
      "complianceState": ""
    },
    {
      "userPrincipalName": "tz.test@contoso.com",
      "userDisplayName": "TZ Test User",
      "ipAddress": "203.0.113.3",
      "country": "DE",
      "city": "Berlin",
      "timestamp": "2026-03-19T12:00:00Z",
      "appDisplayName": "SharePoint",
      "clientAppUsed": "Browser",
      "isLegacyAuth": false,
      "signInStatus": "Success",
      "errorCode": "0",
      "riskLevel": "none",
      "deviceName": "",
      "deviceOS": "",
      "complianceState": ""
    },
    {
      "userPrincipalName": "tz.test@contoso.com",
      "userDisplayName": "TZ Test User",
      "ipAddress": "203.0.113.4",
      "country": "JP",
      "city": "Tokyo",
      "timestamp": "2026-03-20T00:15:00+09:00",
      "appDisplayName": "Azure Portal",
      "clientAppUsed": "Browser",
      "isLegacyAuth": false,
      "signInStatus": "Failure",
      "errorCode": "50126",
      "riskLevel": "low",
      "deviceName": "",
      "deviceOS": "",
      "complianceState": ""
    },
    {
      "userPrincipalName": "tz.test@contoso.com",
      "userDisplayName": "TZ Test User",
      "ipAddress": "203.0.113.5",
      "country": "BR",
      "city": "Sao Paulo",
      "timestamp": "2026-03-19T23:59:59Z",
      "appDisplayName": "Exchange Online",
      "clientAppUsed": "Browser",
      "isLegacyAuth": false,
      "signInStatus": "Success",
      "errorCode": "0",
      "riskLevel": "none",
      "deviceName": "",
      "deviceOS": "",
      "complianceState": ""
    }
  ]
}
```

Document the expected filter behavior in a comment block at the top of the file is not possible in JSON, so instead add a `_test_notes` field inside metadata:

```json
"_test_notes": "Event 1: -04:00 offset = UTC 2026-03-20T03:45. Event 2: +05:00 offset = UTC 2026-03-19T20:30. Event 3: Z = UTC 2026-03-19T12:00. Event 4: +09:00 offset = UTC 2026-03-19T15:15. Event 5: Z = UTC 2026-03-19T23:59:59. Filter dateTo=2026-03-19 should include events 2,3,4,5 and EXCLUDE event 1. Filter dateFrom=2026-03-20 should include ONLY event 1."
```

Add that `_test_notes` field inside the `metadata` object.
  </action>
  <verify>
    <automated>powershell -Command "try { $j = Get-Content 'F:/ai/TheSmith/tests/fixtures/sample-results-tz-offsets.json' -Raw | ConvertFrom-Json; if ($j.results.Count -eq 5 -and $j.results[0].timestamp -match '-04:00' -and $j.results[1].timestamp -match '\+05:00') { 'PASS: Fixture valid with offset timestamps' } else { 'FAIL: Fixture structure incorrect' } } catch { 'FAIL: Invalid JSON' }"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at tests/fixtures/sample-results-tz-offsets.json
    - JSON is valid (ConvertFrom-Json does not throw)
    - Contains exactly 5 result objects
    - At least 2 results have non-Z timezone offsets (one negative, one positive)
    - At least 1 result has a Z-suffixed timestamp
    - metadata._test_notes field documents expected filter behavior
    - All result objects have the same field set as sample-results.json (userPrincipalName, userDisplayName, ipAddress, country, city, timestamp, appDisplayName, clientAppUsed, isLegacyAuth, signInStatus, errorCode, riskLevel, deviceName, deviceOS, complianceState)
  </acceptance_criteria>
  <done>A test fixture with timezone-offset timestamps exists for documenting and manually verifying the date filter edge cases.</done>
</task>

</tasks>

<verification>
1. In web/app.js, `getFilteredSortedEvents()` no longer uses string comparison for date filtering
2. `new Date(evt.timestamp).getTime()` is used for both dateFrom and dateTo comparisons
3. Date boundary sentinels use explicit UTC suffixes (`T00:00:00Z` and `T23:59:59.999Z`)
4. All other filters (search, country, status) and sort logic are unchanged
5. Test fixture documents edge cases with verifiable expected outcomes
</verification>

<success_criteria>
- The date filter in getFilteredSortedEvents() uses epoch-based comparison via new Date().getTime()
- A timestamp like "2026-03-19T23:45:00-04:00" is correctly treated as a March 20 UTC event
- A timestamp like "2026-03-20T01:30:00+05:00" is correctly treated as a March 19 UTC event
- Z-suffixed timestamps continue to work identically (no regression)
- Test fixture with offset timestamps exists for verification
</success_criteria>

<output>
After completion, create `.planning/phases/05-fix-date-range-filter-timezone-offsets/05-01-SUMMARY.md`
</output>
